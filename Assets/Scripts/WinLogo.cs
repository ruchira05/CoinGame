// Win logo script for disappear win logo
using UnityEngine;

public class WinLogo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        gameObject.SetActive(false);
    }
    
}
